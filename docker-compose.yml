version: "3.9"

services:

  frontend:
    build: ./frontend
    container_name: frontend
    expose:
      - "3000:3000"

  backend:
    build: ./web-portfolio-api
    container_name: backend
    expose:
      - "8080"

