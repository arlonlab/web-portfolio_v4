version: "3.9"
services:
  backend:
    build: ./web-portfolio-api
    container_name: backend
    ports:
      - "8080:8080"
    restart: always

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    restart: always